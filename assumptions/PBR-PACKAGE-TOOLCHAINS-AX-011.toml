schema = "proofbound-assumption/1"
id = "PBR-PACKAGE-TOOLCHAINS-AX-011"
statement = "The identified Python runtime, Cargo package, publish, and install commands, archive implementation, compiler, and filesystem preserve the registered verifier and Rust SDK package preflight, reproduction, publication-client, extraction, installation, and execution behavior."
category = "compiler-tcb"
owner = "Proofbound Runtime release maintainer"
rationale = "The package evidence and publication path execute Python and Cargo tooling and observe their outputs. This repository does not prove those tools, their registry clients, or the host filesystem."
scope = "Only the verifier package-production and unrelated-consumer interpretation of PBR-DISTRIBUTION-015 and the Rust package production, publication, and observation paths for PBR-DISTRIBUTION-018."
affected_claims = ["PBR-DISTRIBUTION-015", "PBR-DISTRIBUTION-018"]
review_evidence = [
  ".github/workflows/ci.yml",
  ".github/workflows/release.yml",
  "Cargo.lock",
  "rust-toolchain.toml",
  "tools/release/build_provenance.py",
  "tools/release/build_sdks.py",
  "tools/release/build_verifier_package.py",
  "tools/release/check_verifier_package.py",
  "tools/release/verify_verifier_package_manifest.py",
  "tools/release/verify_provenance.py",
  "tools/release/verify_registry_packages.py",
]
discharge_plan = "Pin exact release tools, reproduce the package in independent directories, retain the package bytes and source revision, and keep this premise visible after exact artifact binding."
source_citation = "docs/specs/0014_public_compatibility_and_distribution.md#7-release-production"
status = "active"
