schema = "proofbound-assumption/1"
id = "PBR-STRING-REP-004"
statement = "Every mirrored authority byte vector presented to translated normalization fits the Rust usize carrier represented by Aeneas alloc.vec.Vec."
category = "representation-premise"
owner = "Proofbound Runtime formalization maintainer"
rationale = "Aeneas represents each translated Vec with an invariant proving its length is at most Usize.max. The registered carrier theorem establishes the bound for every path and environment byte vector in every translated AuthorityPlan."
scope = "Only the source-refinement edge from the registered Rust authority normalizer to PBR-AUTH-001."
affected_claims = ["PBR-AUTH-001"]
review_evidence = [
  "formal/ProofboundRuntime/Refinement/AuthorityNormalization.lean",
  "formal/ProofboundRuntime/Refinement/AuthorityCarrierClaim.lean",
]
discharge_plan = "Discharged by the unconditional registered normalization theorem, whose proof derives every authority byte-carrier bound from the generated Aeneas Vec invariants."
source_citation = "formal/ProofboundRuntime/Refinement/AuthorityCarrierClaim.lean#authority_byte_carriers_bounded"
status = "discharged"
premise_scope = { kind = "all-registered-inputs" }
discharge = { theorem = "authority-normalization-refinement-theorem", scope = { kind = "all-registered-inputs" } }
