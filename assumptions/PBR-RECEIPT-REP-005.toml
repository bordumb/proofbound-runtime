schema = "proofbound-assumption/1"
id = "PBR-RECEIPT-REP-005"
statement = "The Aeneas receipt vector carrier and reviewed Vec::is_empty bridge preserve the exact ordered reason sequence returned by the translated Rust eligibility function."
category = "representation-premise"
owner = "Proofbound Runtime formalization maintainer"
rationale = "Aeneas represents each translated Vec with a bounded list. The registered refinement theorem unfolds every bounded push and the reviewed emptiness bridge, then proves equality with the model reason list for every registered input."
scope = "Only the source-refinement edge from the registered Rust receipt eligibility function to PBR-RECEIPT-004."
affected_claims = ["PBR-RECEIPT-004"]
review_evidence = [
  "formal/ProofboundRuntime/Refinement/ReceiptEligibility.lean",
  "formal/ProofboundRuntime/Refinement/ReceiptEligibilityClaim.lean",
  "formal/bridges/ProofboundRuntimeReceipt/FunsExternal.lean",
]
discharge_plan = "Discharged by the unconditional registered receipt refinement theorem over every translated receipt fact."
source_citation = "formal/ProofboundRuntime/Refinement/ReceiptEligibilityClaim.lean#derive_receipt_eligibility_refines"
status = "discharged"
premise_scope = { kind = "all-registered-inputs" }
discharge = { theorem = "receipt-eligibility-refinement-theorem", scope = { kind = "all-registered-inputs" } }
