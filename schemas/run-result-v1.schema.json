{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://proofbound.dev/runtime/run-result-v1.schema.json",
  "title": "Proofbound Runtime run result version 1",
  "type": "object",
  "additionalProperties": false,
  "required": ["commitment", "execution_id", "outcome", "receipt", "schema"],
  "properties": {
    "commitment": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    },
    "execution_id": {
      "type": "string",
      "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
    },
    "outcome": {
      "oneOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["code", "kind"],
          "properties": {
            "code": {"type": "integer", "minimum": 0, "maximum": 255},
            "kind": {"const": "exited"}
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["kind", "signal"],
          "properties": {
            "kind": {"const": "signaled"},
            "signal": {"type": "integer", "minimum": 1}
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["kind"],
          "properties": {
            "kind": {
              "enum": ["denied", "incomplete", "launcher-failed", "timed-out"]
            }
          }
        }
      ]
    },
    "receipt": {"type": "string", "minLength": 1},
    "schema": {"const": "proofbound-runtime-run-result/1"}
  }
}
