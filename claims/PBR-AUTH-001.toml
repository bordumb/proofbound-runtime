schema = "proofbound-claim/1"
id = "PBR-AUTH-001"
title = "Authority normalization does not amplify authority"
statement = "For every valid authority plan, normalization returns a duplicate-free plan and every returned authority entry was present in the input plan."
public_language = "A kernel-checked theorem directly over the deterministic Aeneas translation proves that authority normalization removes duplicate entries, never adds an entry, and preserves resource limits and network mode exactly. A separate kernel-checked theorem discharges the translated byte-carrier premise for every registered input."
subject = "rust:proofbound_runtime_core::normalize_authority"
profile = "kernel-with-assumptions"
tier = 3
primary_linkage = "refined"
formal_declaration = "ProofboundRuntime.Refinement.AuthorityNormalizationClaim.normalize_authority_refines"
statement_encoding = "lean-expr-cbor/1"
statement_sha256 = "sha256:5f6b14cbe8d5d0b88cf4d79dbbf305b0607e78c4ecff059daac87ef9d292f818"
foundational_axioms = ["Classical.choice", "Quot.sound", "propext"]
evidence = [
  "example-test:authority-conformance",
  "bounded-check:authority-normalization-bounds",
  "theorem:authority-normalization-refinement-theorem",
  "source-refinement:authority-normalization-refinement",
]
assumptions = ["PBR-TOOLCHAIN-AX-003"]
premises = ["PBR-STRING-REP-004"]
open_obligations = [
  "Bind the refined claim to release artifacts before making a shipping-binary claim.",
]
out_of_scope = [
  "Correctness of any Linux enforcement mechanism that later consumes the normalized plan.",
  "Whether the authority requested by a caller is appropriate for the caller's real-world intent.",
]
source_roots = [
  "crates/proofbound-runtime-core/src/authority.rs",
  "crates/proofbound-runtime-core/src/normalize.rs",
  "formal/ProofboundRuntime/Refinement/Authority.lean",
  "formal/ProofboundRuntime/Refinement/AuthorityNormalization.lean",
  "formal/ProofboundRuntime/Refinement/AuthorityCarrierClaim.lean",
  "formal/ProofboundRuntime/Refinement/AuthorityNormalizationClaim.lean",
  "formal/bridges/ProofboundRuntimeCore/FunsExternal.lean",
  "formal/generated/ProofboundRuntimeCore/",
  "assumptions/PBR-STRING-REP-004.toml",
  "proofbound/evidence/authority-conformance.toml",
  "proofbound/evidence/authority-normalization-bounds.toml",
  "proofbound/evidence/authority-normalization-refinement-theorem.toml",
  "proofbound/evidence/authority-normalization-refinement.toml",
  "proofbound/translations/authority-normalization.toml",
  "proofbound/model-checks/authority-normalization.toml",
  "tests/conformance/",
]

[bounded_domain]
id = "authority-two-path-two-environment-catalog"
description = "All two-slot path and environment plans over two typed entries per slot; 2^4 finite plans, including duplicate and reversed path cases."
cardinality = 16
ordering_key = [0, 1, 2, 3]
