schema = "proofbound-claim/1"
id = "PBR-NETWORK-038"
title = "Single-service connector bounds DNS and authenticates the declared TLS name"
statement = "The proposed connector engine takes one complete validated service authority, sends DNS only to its declared numeric resolver over TCP under absolute deadlines, validates query-bound responses and a bounded loop-free CNAME and canonical address result whose effective lifetime includes every alias, attempts only that authority's ordered answer prefix, authenticates an exact non-wildcard DNS SAN with caller-supplied trust-root bytes, an explicit ring provider, and the declared minimum TLS version while disabling resumption and early data, and exposes only one duration-bounded and directionally byte-bounded opaque plaintext session without reconnect or application parsing."
public_language = "Runtime contains a proposed production connector engine for one authenticated service session. Registered tests and an independent source-mutation checker cover its closed error vocabulary, complete-authority binding, DNS wire parser, transaction and TTL rejection, CNAME conflict and effective lifetime behavior, deterministic duplicate-address handling, canonical address order, exact non-wildcard DNS-name call, explicit provider selection, absolute deadline calls, trust-root fail-closed behavior, directional limits, and endpoint-result classification. This is Tier 1 bounded source evidence. It does not yet establish a supervised connector process, launcher binding, child syscall isolation, native enforcement, receipt production, or release-artifact behavior."
subject = "rust:proofbound_runtime_connector::{resolve_service,authenticate_service,proxy_authenticated_channel,DnsResolution}"
profile = "ledger"
tier = 1
evidence = [
  "example-test:authenticated-service-connector-engine",
  "independent-check:authenticated-service-connector-source-contract",
]
assumptions = [
  "PBR-DNS-AX-004",
  "PBR-TLS-AX-005",
]
open_obligations = [
  "Run the connector as one separately identified and supervised process with an exact executable and runtime-closure identity.",
  "Resolve, revalidate, and bind the declared resolver-configuration and trust-root artifacts before supplying their bytes to the connector engine.",
  "Bind successful and failed DNS, endpoint, TLS, channel, counter, and cleanup observations into the service launcher acknowledgement and production receipt.",
  "Run the complete resolver, TLS, inherited-channel, direct-network, limit, crash, and cleanup attack corpus on x86_64 and aarch64 release artifacts.",
  "Independently review the rustls version, crypto-provider configuration, trust-root parsing, DNS parser, proxy loop, and every effectful error path.",
]
out_of_scope = [
  "Any claim that the production CLI accepts service-session plans or that a launcher or child uses this engine.",
  "Correctness of the resolver, DNS transport, rustls, ring, x509-cert, certificate authorities, remote service, kernel, hardware, or host operator.",
  "HTTP, LLM, request, response, credential, redirect, proxy, multiple-session, reconnect, QUIC, or application-operation semantics.",
]
source_roots = [
  "Cargo.lock",
  "Cargo.toml",
  "assumptions/PBR-DNS-AX-004.toml",
  "assumptions/PBR-TLS-AX-005.toml",
  "claims/PBR-NETWORK-038.toml",
  "crates/proofbound-runtime-connector/Cargo.toml",
  "crates/proofbound-runtime-connector/src/dns.rs",
  "crates/proofbound-runtime-connector/src/lib.rs",
  "crates/proofbound-runtime-connector/src/tls.rs",
  "crates/proofbound-runtime-binding/Cargo.toml",
  "crates/proofbound-runtime-binding/src/lib.rs",
  "crates/proofbound-runtime-core/Cargo.toml",
  "crates/proofbound-runtime-core/src/authority.rs",
  "crates/proofbound-runtime-core/src/diagnostic.rs",
  "crates/proofbound-runtime-core/src/error.rs",
  "crates/proofbound-runtime-core/src/identity.rs",
  "crates/proofbound-runtime-core/src/lib.rs",
  "crates/proofbound-runtime-core/src/network.rs",
  "crates/proofbound-runtime-core/src/normalize.rs",
  "crates/proofbound-runtime-core/src/outcome.rs",
  "crates/proofbound-runtime-core/src/plan.rs",
  "crates/proofbound-runtime-core/src/policy.rs",
  "crates/proofbound-runtime-core/src/receipt.rs",
  "crates/proofbound-runtime-core/src/run_result.rs",
  "crates/proofbound-runtime-core/src/service_lifecycle.rs",
  "crates/proofbound-runtime-core/src/service_policy.rs",
  "crates/proofbound-runtime-core/src/wire_v2.rs",
  "crates/proofbound-runtime-receipt/Cargo.toml",
  "crates/proofbound-runtime-receipt/src/lib.rs",
  "deny.toml",
  "docs/adr/0004-authenticated-service-session.md",
  "docs/specs/0016_authenticated_service_session.md",
  "proofbound/evidence/authenticated-service-connector-engine.toml",
  "proofbound/evidence/authenticated-service-connector-source-contract.toml",
  "tools/ci/check_connector_source_contract.py",
  "tools/ci/test_connector_source_contract.py",
]
