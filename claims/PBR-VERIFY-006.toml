schema = "proofbound-claim/1"
id = "PBR-VERIFY-006"
title = "The independent verifier rejects registered receipt attacks"
statement = "The standalone verifier independently derives receipt validity and reuse eligibility and rejects every registered receipt mutation with its exact typed reason."
subject = "planned:proofbound_runtime_verify::verify_receipt"
profile = "ledger"
tier = 0
evidence = ["example-test:receipt-eligibility-verifier-conformance"]
assumptions = ["PBR-TOOLCHAIN-AX-003"]
open_obligations = [
  "Implement the independent receipt decoder, canonical-byte checks, identity validation, and verifier CLI.",
  "Freeze shared adversarial receipt vectors and register exact rejection reasons.",
  "Bind the independent verifier to exact release artifacts.",
]
out_of_scope = [
  "Correctness of the receipt producer merely because the verifier accepts one bounded corpus.",
  "Receipt properties not represented by the versioned schema and derivation rules.",
]
source_roots = [
  "crates/proofbound-runtime-verify/src/derive.rs",
  "crates/proofbound-runtime-verify/tests/receipt_vectors.rs",
  "proofbound/evidence/receipt-eligibility-verifier-conformance.toml",
  "schemas/receipt-eligibility-vector-v1.schema.json",
  "tests/conformance/receipt/positive/eligibility-v1.json",
]
